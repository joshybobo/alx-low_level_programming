#include "main.h"

/**
 * _islower - checks for lowercase character
 * @u: the character to check
 * Return: 1 if u is lowercase, 0 otherwise
 */
int _islower(int u)
{
	return (u >= 'a' && u <= 'z');
}
